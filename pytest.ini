[pytest]
minversion = 7.0
testpaths = tests
python_files = test_*.py
# -n auto: use all available CPU cores (disable with -n0 when debugging)
# --dist=loadscope: keeps tests within the same module on the same worker
addopts = -q -n auto --dist=loadscope
