[pytest]
minversion = 7.0
testpaths = tests
python_files = test_*.py
addopts = -q -n auto --dist=loadscope
filterwarnings =
    ignore::DeprecationWarning
    ignore::RuntimeWarning
    ignore::PendingDeprecationWarning
markers =
    governance: marks tests as governance quality gate checks
